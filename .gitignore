# Dépendances
node_modules

# Build
dist

# IDE & OS
.vscode
.DS_Store
Thumbs.db

# Claude Code — config locale (chemins absolus, permissions machine-specific)
.claude/

# Android Keystore — NE JAMAIS COMMITTER
*.jks
*.keystore
android/keystore.properties

# Variables d'environnement — NE JAMAIS COMMITTER (clés API, secrets)
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Tuiles overview générées (build-overview-tiles.ts)
public/tiles/*.pmtiles

# Vite
*.local

# Screenshots de debug (générés par les outils IA/devtools)
debug_*.png
startup_screenshot.png

# Artefacts Windows
nul
%TEMP%*/

# Dépôts de référence clonés localement par les agents IA (lecture seule, non committer)
cesium/
deck.gl/
maplibre-gl-js/
%TEMP%cesium/
%TEMP%mapboxgl/
%TEMP%threejs/

# Android Studio — fichiers de session IDE (changent à chaque déploiement)
android/.idea/deploymentTargetSelector.xml
android/.idea/workspace.xml

# Plans internes Sisyphus IA (orchestration — non pertinent pour le repo)
.sisyphus/