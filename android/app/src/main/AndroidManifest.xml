<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:largeHeap="true">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            TrackingActivity — Activity transparente dans le processus :tracking (v5.53.0).
            Ce processus est indépendant du processus principal (MainActivity).
            Quand l'utilisateur kill l'app principale, RecordingService continue dans :tracking.
        -->
        <activity
            android:name=".TrackingActivity"
            android:process=":tracking"
            android:theme="@android:style/Theme.NoDisplay"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true" />

        <!--
            Foreground Service GPS dans le processus :tracking (v5.53.0).
            android:process=":tracking" — processus séparé du processus principal.
            android:stopWithTask="false" — survit au swipe de l'app des recents.
            Avec android:process séparé, survivra même au kill du processus principal.
        -->
        <service
            android:name=".RecordingService"
            android:process=":tracking"
            android:foregroundServiceType="location"
            android:stopWithTask="false"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- profileable retiré des builds production (audit sécurité v5.22.1) -->
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Foreground Service GPS (enregistrement de parcours en arrière-plan) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Notifications (Android 13+ / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Google Play Billing — requis pour les achats In-App (RevenueCat) -->
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- Wake Lock — maintient le CPU actif pendant l'enregistrement GPS (v5.23) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- AlarmManager pour relancer RecordingService si swipé par OEM agressif (v5.53.1) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Exemption batterie — l'utilisateur est invité à désactiver l'optimisation batterie pour garantir le GPS long (v5.53.2) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
</manifest>
