name: Full browser E2E

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  browsers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Disable unrelated Google Chrome apt source
        shell: bash
        run: |
          # The hosted image does not need this repository for Playwright browsers.
          # Removing it prevents stale Chrome metadata from breaking apt update.
          sudo rm -f \
            /etc/apt/sources.list.d/google-chrome.list \
            /etc/apt/sources.list.d/google-chrome.list.save \
            /etc/apt/sources.list.d/google-chrome.sources
      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium firefox webkit
      - run: npm run test:e2e:full
        env:
          # Valeurs inertes : les scénarios E2E mockent l'authentification si nécessaire.
          VITE_SUPABASE_URL: https://placeholder.supabase.co
          VITE_SUPABASE_ANON_KEY: e2e-placeholder-anon-key
      - name: Upload E2E diagnostics
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-full-report
          path: |
            playwright-report
            test-results
